{
  "name": "class-work-transfer-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Test 2 — composable autonomous transfer agent using starknet-agentic MCP",
  "type": "module",
  "scripts": {
    "run": "npx tsx run.ts",
    "run:dry": "TRANSFER_EXECUTE=0 npx tsx run.ts",
    "run:execute": "TRANSFER_EXECUTE=1 npx tsx run.ts",
    "send": "TRANSFER_EXECUTE=1 npx tsx run.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "dotenv": "^17.4.2",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.22.3",
    "typescript": "^5.7.0"
  }
}
